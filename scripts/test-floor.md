The number of tests that must pass. See scripts/run-tests.

Set to the exact current count, not a round number below it: the point is that
losing a single test fails CI. Adding tests is free; removing one is a diff
somebody has to justify.
